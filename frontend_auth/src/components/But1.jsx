import React from 'react'

export default function But1({onClick}) {
  return (
    
    <button type='submit' onClick={onclick} className="btn btn-wide bg-black  text-white hover:bg-black hover:text-white w-full">Log in</button>
    
  )
}
